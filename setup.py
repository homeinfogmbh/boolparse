#! /usr/bin/env python3

from distutils.core import setup

setup(
    name='boolparse',
    version='latest',
    author='Richard Neumann',
    py_modules=['boolparse'],
    license=open('LICENSE').read(),
    description='A library for boolean expression parsing')
